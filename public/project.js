"use strict";
const data=window.TRPGProjectData,$=selector=>document.querySelector(selector);let pendingIcon="";
const BOARDS_KEY="jijinboardOwnedBoards.v1";
function escapeHtml(value=""){return String(value).replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]))}
function readBoards(){return data.read(BOARDS_KEY,{})}function saveBoards(value){return data.write(BOARDS_KEY,value)}
function ownerId(){let id=localStorage.getItem("jijinboardOwnerId");if(!id){id=crypto.randomUUID?.()||`owner_${Date.now()}_${Math.random().toString(36).slice(2)}`;localStorage.setItem("jijinboardOwnerId",id)}return id}
async function api(path,options={}){const response=await fetch(path,{headers:{"content-type":"application/json",...(options.headers||{})},...options});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||`通信エラー (${response.status})`);return body}
async function compressPersonIcon(file){if(!file)return"";if(typeof createImageBitmap!=="function")return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file)});let bitmap=null;try{bitmap=await createImageBitmap(file);const size=256,scale=Math.min(1,size/Math.max(bitmap.width,bitmap.height)),w=Math.max(1,Math.round(bitmap.width*scale)),h=Math.max(1,Math.round(bitmap.height*scale)),canvas=document.createElement("canvas");canvas.width=w;canvas.height=h;const ctx=canvas.getContext("2d",{alpha:true});ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality="high";ctx.clearRect(0,0,w,h);ctx.drawImage(bitmap,0,0,w,h);return canvas.toDataURL("image/webp",.84)}finally{try{bitmap?.close?.()}catch{}}}
function initials(name){return [...String(name||"?")].slice(0,2).join("")}function iconMarkup(person){return person.icon?`<img class="person-icon" src="${escapeHtml(person.icon)}" alt="">`:`<span class="person-icon">${escapeHtml(initials(person.name))}</span>`}
const autoSpreadsheetKeys=new Set(["id","projectPersonId","name","alias","key","base","sources","local"]);
function isAutoSpreadsheetCharacter(ch){return !!ch&&ch.local===true&&ch.base==null&&Array.isArray(ch.sources)&&ch.sources.length===0&&!String(ch.alias||"").trim()&&!!ch.projectPersonId&&ch.id===ch.projectPersonId&&Object.keys(ch).every(key=>autoSpreadsheetKeys.has(key))}
function parseLocal(key,fallback){try{return JSON.parse(localStorage.getItem(key)||"")??fallback}catch{return fallback}}
function actualLogPersonIds(){const ids=new Set();for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i);if(!key?.startsWith("personas:"))continue;const personas=parseLocal(key,[]);if(!Array.isArray(personas))continue;for(const persona of personas)if(persona?.projectPersonId)ids.add(String(persona.projectPersonId))}return ids}
function legacySpreadsheetPersonIds(){const ids=new Set();for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i);if(key!=="charaHub.characters"&&!/:spreadsheet:charaHub\.characters$/.test(String(key||"")))continue;const chars=parseLocal(key,[]);if(!Array.isArray(chars))continue;for(const ch of chars)if(ch?.projectPersonId&&!isAutoSpreadsheetCharacter(ch))ids.add(String(ch.projectPersonId))}return ids}
function cleanupLegacySpreadsheetPeople(){const linked=legacySpreadsheetPersonIds();if(!linked.size)return;const logIds=actualLogPersonIds(),people=data.people(),next=people.filter(person=>!linked.has(String(person?.id||""))||logIds.has(String(person?.id||"")));if(next.length!==people.length)data.savePeople(next)}
async function deleteBoard(boardId,boardName){const saved=readBoards(),entry=saved[boardId];if(!entry)return;if(!confirm(`「${boardName||entry.name||"この自陣"}」を完全に削除しますか？\n\nこの自陣のLOG・MATRIX・Spreadsheet・COMMENTSも削除され、元に戻せません。\n共通のPL / PC / NPCマスターは残ります。`))return;const token=String(entry.adminToken||localStorage.getItem(`boardAdmin:${boardId}`)||"");if(!token)throw new Error("この自陣の管理情報がありません");await api(`/api/boards/${encodeURIComponent(boardId)}`,{method:"DELETE",headers:{"x-board-admin-token":token}});delete saved[boardId];saveBoards(saved);localStorage.removeItem(`boardAdmin:${boardId}`);await renderBoards()}
async function renderBoards(){const saved=readBoards(),entries=Object.entries(saved),list=$("#boardList"),status=$("#boardStatus");status.textContent="";if(!entries.length){list.innerHTML="";$("#boardEmpty").hidden=false;return}let remote=[];try{remote=(await api("/api/top-auth/rooms")).rooms||[]}catch(error){status.textContent=error.message}const byId=new Map(remote.map(room=>[String(room.id),room])),boards=entries.map(([id,item])=>{const room=byId.get(String(id));return room?{...room,available:true}:{id,name:item.name||"読み込めない部屋",createdAt:"",logCount:0,available:false}});boards.sort((a,b)=>String(a.createdAt||"").localeCompare(String(b.createdAt||"")));list.innerHTML=boards.map((board,index)=>`<div class="session-card-wrap"><a class="session-card ${board.available?"":"unavailable"}" ${board.available?`href="/board/?id=${encodeURIComponent(board.id)}"`:""}><span>ROOM ${String(index+1).padStart(2,"0")}</span><strong>${escapeHtml(board.name)}</strong><small>${board.available?`${Number(board.logCount)||0} LOGS`:"— LOGS"}</small><i>${board.available?"開く ›":"読み込めません"}</i></a>${board.available?`<button class="room-delete-button" type="button" data-delete-board="${escapeHtml(board.id)}" data-board-name="${escapeHtml(board.name)}" title="この自陣を削除" aria-label="${escapeHtml(board.name)}を削除">×</button>`:""}</div>`).join("");$("#boardEmpty").hidden=boards.length>0;list.querySelectorAll("[data-delete-board]").forEach(button=>button.onclick=async event=>{event.preventDefault();event.stopPropagation();button.disabled=true;try{await deleteBoard(button.dataset.deleteBoard,button.dataset.boardName)}catch(error){status.textContent=error.message;button.disabled=false}})}
function renderPeople(){cleanupLegacySpreadsheetPeople();const people=data.importLogPeople(),root=$("#peopleGroups"),plById=new Map(people.filter(item=>item.type==="PL").map(item=>[item.id,item]));root.innerHTML=["PL","PC","NPC"].map(type=>{const group=people.filter(item=>item.type===type);if(!group.length)return"";return `<section class="people-group"><h3>${type}</h3><div>${group.map(person=>`<button class="person-row" data-person-id="${escapeHtml(person.id)}">${iconMarkup(person)}<span><strong>${escapeHtml(person.name)}</strong>${person.plId&&plById.get(person.plId)?`<small>${escapeHtml(plById.get(person.plId).name)}</small>`:""}</span><i style="--person-color:${escapeHtml(person.color)}"></i></button>`).join("")}</div></section>`}).join("");$("#peopleEmpty").hidden=people.length>0;root.querySelectorAll("[data-person-id]").forEach(button=>button.onclick=()=>openPersonDialog(button.dataset.personId))}
function openPersonDialog(id=""){const person=data.people().find(item=>item.id===id),pls=data.people().filter(item=>item.type==="PL"&&item.id!==id);$("#personId").value=person?.id||"";$("#personType").value=person?.type||"PC";$("#personName").value=person?.name||"";$("#personColor").value=person?.color||"#ffe66b";pendingIcon=person?.icon||"";$("#personOwner").innerHTML='<option value="">なし</option>'+pls.map(item=>`<option value="${escapeHtml(item.id)}">${escapeHtml(item.name)}</option>`).join("");$("#personOwner").value=person?.plId||"";$("#personIconPreview").innerHTML=pendingIcon?`<img src="${escapeHtml(pendingIcon)}" alt="">`:"";$("#personDialogTitle").textContent=person?"人物を編集":"人物を追加";$("#deletePersonButton").classList.toggle("hidden",!person);syncPersonFields();$("#personDialog").showModal()}
function syncPersonFields(){$("#personOwnerRow").hidden=$("#personType").value==="PL"}
async function syncPlayerMaster(){try{await window.JIJINPlayerMaster?.pushCurrent?.()}catch(error){console.warn("PEOPLE master sync failed",error)}}
async function deletePlayerMasterCharacter(person){if(!person||person.type==="PL")return;const playerId=String(window.JIJINPlayerMaster?.playerId||localStorage.getItem("jijinboardPlayerMasterId.v1")||""),accessToken=String(localStorage.getItem("jijinboardPlayerMasterToken.v1")||"");if(!playerId||!accessToken)return;try{const response=await fetch(`/api/player-master/characters/${encodeURIComponent(person.id)}?playerId=${encodeURIComponent(playerId)}`,{method:"DELETE",headers:{"content-type":"application/json","x-player-token":accessToken}});if(!response.ok){const body=await response.json().catch(()=>({}));throw new Error(body.error||"PCマスターを削除できませんでした")}}catch(error){console.warn("Player master delete failed",error)}}
async function deleteRemotePerson(result){const profile=data.read("trpgMarkerProfile",null),authorId=String(profile?.id||"");if(!authorId)return;const targetName=String(result?.target?.name||""),removedByRoom=new Map();for(const ref of result?.removedPersonas||[]){if(!ref.roomId||!ref.personaId)continue;const set=removedByRoom.get(ref.roomId)||new Set();set.add(ref.personaId);removedByRoom.set(ref.roomId,set)}const boards=Object.keys(readBoards());for(const boardId of boards){let board=null;try{board=await api(`/api/boards/${encodeURIComponent(boardId)}`)}catch{continue}for(const log of board.logs||[]){const localIds=removedByRoom.get(log.roomId)||new Set();const matches=(log.participants||[]).filter(person=>person.authorId===authorId&&(localIds.has(String(person.personaId||""))||(targetName&&person.name===targetName)));for(const person of matches){try{await api(`/api/boards/${encodeURIComponent(boardId)}/logs/${encodeURIComponent(log.roomId)}/participants/${encodeURIComponent(person.personaId)}`,{method:"DELETE",body:JSON.stringify({authorId})})}catch(error){console.warn("Remote PC delete failed",error)}}}}}
$("#addBoardButton").onclick=()=>{$("#boardName").value="";$("#boardDialog").showModal()};$("#closeBoardDialog").onclick=()=>$("#boardDialog").close();
$("#boardForm").onsubmit=async event=>{event.preventDefault();const button=event.submitter,name=$("#boardName").value.trim();if(!name)return;button.disabled=true;try{const board=await api("/api/boards",{method:"POST",body:JSON.stringify({name,ownerId:ownerId()})}),saved=readBoards();saved[board.id]={name:board.name,adminToken:board.adminToken};saveBoards(saved);localStorage.setItem(`boardAdmin:${board.id}`,board.adminToken);location.href=`/board/?id=${encodeURIComponent(board.id)}`}catch(error){$("#boardStatus").textContent=error.message;button.disabled=false}};
$("#addPersonButton").onclick=()=>openPersonDialog();$("#closePersonDialog").onclick=()=>$("#personDialog").close();$("#personType").onchange=syncPersonFields;
$("#personIcon").onchange=async event=>{const file=event.target.files?.[0];if(!file)return;try{pendingIcon=await compressPersonIcon(file);$("#personIconPreview").innerHTML=`<img src="${escapeHtml(pendingIcon)}" alt="">`}catch(error){console.warn("Person icon compression failed",error)}};
$("#personForm").onsubmit=async event=>{event.preventDefault();const name=$("#personName").value.trim();if(!name)return;data.upsertPerson({id:$("#personId").value,type:$("#personType").value,name,icon:pendingIcon,color:$("#personColor").value,plId:$("#personType").value==="PL"?"":$("#personOwner").value});$("#personDialog").close();renderPeople();await syncPlayerMaster()};
$("#deletePersonButton").onclick=async()=>{const id=$("#personId").value,person=data.people().find(item=>item.id===id);if(!id||!person||!confirm(`「${person.name}」を完全に削除しますか？\n\nLOG / MAGIA MATRIXのPC登録と復旧用データからも削除します。\n過去のCOMMENTS本文は残ります。`))return;const button=$("#deletePersonButton");button.disabled=true;const result=data.removePersonDeep(id);$("#personDialog").close();renderPeople();try{await Promise.allSettled([deletePlayerMasterCharacter(person),deleteRemotePerson(result)])}finally{button.disabled=false}};
window.addEventListener("jijinboard-player-master-updated",renderPeople);
ownerId();renderBoards();renderPeople();
if(!document.querySelector('script[data-jijin-player-master]')){const script=document.createElement('script');script.src='/shared/player-master.js?v=20260830-3';script.dataset.jijinPlayerMaster='1';document.body.append(script)}
