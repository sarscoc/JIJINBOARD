"use strict";

const $=selector=>document.querySelector(selector),params=new URL(location.href).searchParams,boardId=params.get("id");
const state={board:null,tool:"log",activeRoom:params.get("room")||"",pendingRoom:"",editingRoom:"",summaries:{},opened:{},profile:null};

function esc(value=""){return String(value).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
async function api(path,options={}){const response=await fetch(path,{headers:{"content-type":"application/json",...(options.headers||{})},...options}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||`通信エラー (${response.status})`);return body}
function adminToken(){return localStorage.getItem(`boardAdmin:${boardId}`)||JSON.parse(localStorage.getItem("jijinboardOwnedBoards.v1")||"{}")[boardId]?.adminToken||""}
function openedKey(){return `jijinboardOpenedLogs:${boardId}`}
function saveOpened(){try{sessionStorage.setItem(openedKey(),JSON.stringify(state.opened))}catch{}}
function loadOpened(){try{return JSON.parse(sessionStorage.getItem(openedKey())||"{}")||{}}catch{return{}}}
function participantSyncKey(roomId){return `jijinboardParticipantSync:${boardId}:${roomId}`}
function participantSignature(profile){const personas=(profile?.personas||[]).filter(persona=>persona.type==="PC").map(persona=>({id:persona.id||"",name:persona.name||"",type:persona.type||"PC",icon:persona.icon||""}));return JSON.stringify({authorId:profile?.id||"",plName:profile?.plName||"",personas})}
function setLogActive(frame,active){try{const win=frame?.contentWindow;if(!win)return;if(active)win.connectRealtime?.();else win.disconnectRealtime?.();win.postMessage({type:"jijinboard-log-active",active},location.origin)}catch{}}
function setToolFrameLoading(frame){if(!frame)return;frame.dataset.ready="";frame.style.visibility="hidden";const stamp=String(Date.now())+Math.random();frame.dataset.loadStamp=stamp;setTimeout(()=>{if(frame.dataset.loadStamp===stamp&&frame.dataset.ready!=="1")frame.style.visibility=""},8000)}
function setToolFrameReady(frame){if(!frame)return;frame.dataset.ready="1";frame.style.visibility=""}
async function summary(roomId){if(!state.summaries[roomId])state.summaries[roomId]=await api(`/api/rooms/${encodeURIComponent(roomId)}?summary=1`);return state.summaries[roomId]}
function participantMarkup(people=[]){return people.length?`<small class="log-participants">${people.slice(0,5).map(person=>person.icon?`<img src="${esc(person.icon)}" alt="">`:`<i>${esc((person.name||"?").slice(0,1))}</i>`).join("")}<b>${esc(people.map(person=>person.name).join(" / "))}</b></small>`:""}
function renderLogs(){const logs=state.board.logs||[],owner=!!adminToken();$("#emptyLogs").hidden=logs.length>0;$("#logList").innerHTML=logs.map((item,index)=>{const known=state.opened[item.roomId],active=item.roomId===state.activeRoom,title=state.summaries[item.roomId]?.title||item.title||known||"ログ";return `<div class="log-entry"><button class="log-item ${item.spoiler&&!known?"unopened":""} ${active?"active":""}" data-room="${esc(item.roomId)}"><span>LOG ${String(index+1).padStart(2,"0")}</span><strong>${esc(title)}</strong>${participantMarkup(item.participants)}</button>${owner?`<button class="log-edit" data-edit-log="${esc(item.roomId)}" title="ログの編集">✎</button>`:""}</div>`}).join("");$("#logList").querySelectorAll("[data-room]").forEach(button=>button.onclick=()=>requestOpen(button.dataset.room));$("#logList").querySelectorAll("[data-edit-log]").forEach(button=>button.onclick=event=>{event.stopPropagation();openLogEdit(button.dataset.editLog)})}
function closeSpoiler(){const dialog=$("#spoilerDialog");if(dialog.open)dialog.close();state.pendingRoom=""}
function requestOpen(roomId){const item=state.board.logs.find(item=>item.roomId===roomId);if(!item)return;if(!item.spoiler||state.opened[roomId])return openLog(roomId);const index=state.board.logs.indexOf(item);state.pendingRoom=roomId;$("#spoilerLabel").textContent=`LOG ${String(index+1).padStart(2,"0")}`;$("#spoilerTitle").textContent=item.scenarioTitle||item.title||"LOG";const people=$("#spoilerParticipants");people.textContent=item.scenarioParticipants?`参加PC：${item.scenarioParticipants}`:"";people.classList.toggle("hidden",!item.scenarioParticipants);const dialog=$("#spoilerDialog");if(!dialog.open)dialog.show()}
async function openLog(roomId,title=""){if($("#spoilerDialog").open)closeSpoiler();const item=state.board.logs.find(item=>item.roomId===roomId);if(title)state.opened[roomId]=title;else if(!state.opened[roomId])state.opened[roomId]=item?.title||(await summary(roomId)).title;saveOpened();state.activeRoom=roomId;renderLogs();const url=new URL(location.href);url.searchParams.set("id",boardId);url.searchParams.set("room",roomId);history.replaceState(null,"",url.pathname+url.search);$("#welcome").classList.add("hidden");const frame=$("#logFrame"),src=`/log/?room=${encodeURIComponent(roomId)}&embedded=1&board=${encodeURIComponent(boardId)}`;if(frame.dataset.room!==roomId){setToolFrameLoading(frame);frame.dataset.room=roomId;frame.src=src}selectTool("log")}
function selectTool(tool){
  state.tool=tool;if(tool!=="log")closeSpoiler();
  document.querySelectorAll("[data-tool]").forEach(button=>button.classList.toggle("active",button.dataset.tool===tool));
  const frames={log:$("#logFrame"),matrix:$("#matrixFrame"),spreadsheet:$("#spreadsheetFrame")},hasLog=!!frames.log.getAttribute("src");
  Object.entries(frames).forEach(([name,frame])=>frame.classList.toggle("hidden",name!==tool||(name==="log"&&!hasLog)));
  $("#welcome").classList.toggle("hidden",tool!=="log"||hasLog);
  $("#logSidebar").classList.toggle("tool-hidden",tool==="matrix");
  $(".board-layout").classList.toggle("matrix-open",tool==="matrix");
  if(tool==="matrix"&&!frames.matrix.getAttribute("src")){
    setToolFrameLoading(frames.matrix);
    frames.matrix.src=`/matrix/?embedded=1&board=${encodeURIComponent(boardId)}&room=${encodeURIComponent(state.activeRoom)}`;
  }
  if(tool==="spreadsheet"&&!frames.spreadsheet.getAttribute("src")){
    setToolFrameLoading(frames.spreadsheet);
    frames.spreadsheet.src=`/spreadsheet/?embedded=1&board=${encodeURIComponent(boardId)}`;
  }
  setLogActive(frames.log,tool==="log");
  if(tool==="matrix"&&frames.matrix.getAttribute("src")&&frames.matrix.dataset.ready==="1")frames.matrix.contentWindow?.postMessage({type:"jijinboard-active-room",roomId:state.activeRoom},location.origin);
  frames.matrix.contentWindow?.postMessage({type:"jijinboard-matrix-active",active:tool==="matrix"},location.origin);
  frames.spreadsheet.contentWindow?.postMessage({type:"jijinboard-spreadsheet-active",active:tool==="spreadsheet"},location.origin);
}
async function attachRoom(message){const token=adminToken();if(!token)return;await api(`/api/boards/${encodeURIComponent(boardId)}/logs`,{method:"POST",headers:{"x-board-admin-token":token},body:JSON.stringify({roomId:message.roomId,roomAdminToken:message.adminToken,spoiler:!!message.spoiler,scenarioTitle:message.scenarioTitle||"",scenarioParticipants:message.scenarioParticipants||""})});state.opened[message.roomId]=message.title||"LOG";saveOpened();state.board=await api(`/api/boards/${encodeURIComponent(boardId)}`);openLog(message.roomId,state.opened[message.roomId])}
function openLogEdit(roomId){closeSpoiler();const item=state.board.logs.find(log=>log.roomId===roomId);if(!item)return;state.editingRoom=roomId;$("#logSpoilerInput").checked=!!item.spoiler;$("#logScenarioTitle").value=item.scenarioTitle||"";$("#logScenarioParticipants").value=item.scenarioParticipants||"";$("#logEditDialog").showModal()}
async function saveLogEdit(event){event.preventDefault();const roomId=state.editingRoom;if(!roomId)return;const spoiler=$("#logSpoilerInput").checked;try{await api(`/api/boards/${encodeURIComponent(boardId)}/logs/${encodeURIComponent(roomId)}`,{method:"PATCH",headers:{"x-board-admin-token":adminToken()},body:JSON.stringify({spoiler,scenarioTitle:$("#logScenarioTitle").value,scenarioParticipants:$("#logScenarioParticipants").value})});state.board=await api(`/api/boards/${encodeURIComponent(boardId)}`);if(spoiler){delete state.opened[roomId];saveOpened()}renderLogs();$("#logEditDialog").close();if(spoiler&&state.activeRoom===roomId)requestOpen(roomId)}catch(error){alert(error.message)}}
async function removeLog(){const roomId=state.editingRoom;if(!roomId||!confirm("このログを完全に削除しますか？\nログ本文・感想・MATRIXの配置とコメントも元に戻せません。"))return;try{await api(`/api/boards/${encodeURIComponent(boardId)}/logs/${encodeURIComponent(roomId)}`,{method:"DELETE",headers:{"x-board-admin-token":adminToken()}});["admin:","personas:","mainTab:","hiddenTabs:","theme:"].forEach(prefix=>localStorage.removeItem(prefix+roomId));localStorage.removeItem(participantSyncKey(roomId));delete state.opened[roomId];saveOpened();state.board=await api(`/api/boards/${encodeURIComponent(boardId)}`);if(state.activeRoom===roomId){state.activeRoom="";const frame=$("#logFrame");setLogActive(frame,false);frame.removeAttribute("src");delete frame.dataset.room;frame.dataset.ready="";frame.style.visibility="";$("#welcome").classList.remove("hidden")}$("#logEditDialog").close();renderLogs()}catch(error){alert(error.message)}}
async function syncParticipants(profile,roomId){const personas=(profile?.personas||[]).filter(persona=>persona.type==="PC");if(!roomId||!profile?.plName)return;const signature=participantSignature(profile),key=participantSyncKey(roomId);if(localStorage.getItem(key)===signature)return;await api(`/api/boards/${encodeURIComponent(boardId)}/logs/${encodeURIComponent(roomId)}/participants`,{method:"POST",body:JSON.stringify({authorId:profile.id,plName:profile.plName,personas})});localStorage.setItem(key,signature);state.board=await api(`/api/boards/${encodeURIComponent(boardId)}`);renderLogs();if(state.tool==="matrix")$("#matrixFrame").contentWindow?.postMessage({type:"jijinboard-active-room",roomId},location.origin)}
async function init(){if(!boardId){document.body.innerHTML='<p style="padding:30px">部屋URLが正しくありません。</p>';return}try{state.opened=loadOpened();state.board=await api(`/api/boards/${encodeURIComponent(boardId)}`);$("#boardName").textContent=state.board.name;document.title=`${state.board.name} | JIJINBOARD`;const owner=!!adminToken();$("#ownerTopLink").classList.toggle("hidden",!owner);$("#guestBrand").classList.toggle("hidden",owner);$("#addLogButton").classList.toggle("hidden",!owner);$("#uploadLogButton").classList.toggle("hidden",!owner);renderLogs();if(state.activeRoom&&state.board.logs.some(item=>item.roomId===state.activeRoom))requestOpen(state.activeRoom);else if(state.board.logs?.length===1)requestOpen(state.board.logs[0].roomId)}catch(error){document.body.innerHTML=`<p style="padding:30px">${esc(error.message)}</p>`}}

document.querySelectorAll("[data-tool]").forEach(button=>button.onclick=()=>selectTool(button.dataset.tool));
$("#confirmOpen").onclick=async()=>{const room=state.pendingRoom,title=$("#spoilerTitle").textContent;closeSpoiler();if(room)await openLog(room,title)};
function showUploader(){closeSpoiler();$("#welcome").classList.add("hidden");const frame=$("#logFrame");setLogActive(frame,false);delete frame.dataset.room;setToolFrameLoading(frame);frame.src=`/log/?embedded=1&board=${encodeURIComponent(boardId)}`;selectTool("log")}
$("#addLogButton").onclick=showUploader;$("#uploadLogButton").onclick=showUploader;
$("#closeLogEdit").onclick=()=>$("#logEditDialog").close();$("#logEditForm").onsubmit=saveLogEdit;$("#deleteLogButton").onclick=removeLog;
$("#shareBoard").onclick=async()=>{await navigator.clipboard.writeText(`${location.origin}/board/?id=${encodeURIComponent(boardId)}`);const old=$("#shareBoard").textContent;$("#shareBoard").textContent="コピー済";setTimeout(()=>$("#shareBoard").textContent=old,1400)};
$("#profileButton").onclick=()=>$("#logFrame").contentWindow?.postMessage({type:"jijinboard-open-profile"},location.origin);
$("#logFrame").addEventListener("load",()=>{const frame=$("#logFrame");frame.contentWindow?.postMessage({type:"jijinboard-request-profile"},location.origin);setLogActive(frame,state.tool==="log")});
$("#matrixFrame").addEventListener("load",()=>{const frame=$("#matrixFrame");frame.contentWindow?.postMessage({type:"jijinboard-matrix-active",active:state.tool==="matrix"},location.origin)});
$("#spreadsheetFrame").addEventListener("load",()=>$("#spreadsheetFrame").contentWindow?.postMessage({type:"jijinboard-spreadsheet-active",active:state.tool==="spreadsheet"},location.origin));
addEventListener("message",event=>{if(event.origin!==location.origin)return;const message=event.data||{};if(message.type==="jijinboard-room-created")attachRoom(message).catch(error=>alert(error.message));if(message.type==="jijinboard-profile"){state.profile=message.profile;syncParticipants(message.profile,message.roomId||state.activeRoom).catch(()=>{})}if(message.type==="jijinboard-presence")$("#presenceBar").innerHTML=(message.people||[]).map(person=>`<span class="presence-person">${person.pl_icon?`<img src="${esc(person.pl_icon)}" alt="">`:`<i>${esc((person.pl_name||"?").slice(0,1))}</i>`}<b>${esc(person.pl_name)}</b>${person.is_typing?'<em>入力中…</em>':""}</span>`).join("");if(message.type==="jijinboard-log-ready"&&event.source===$("#logFrame").contentWindow)setToolFrameReady($("#logFrame"));if(message.type==="jijinboard-matrix-ready"&&event.source===$("#matrixFrame").contentWindow)setToolFrameReady($("#matrixFrame"));if(message.type==="jijinboard-spreadsheet-ready"&&event.source===$("#spreadsheetFrame").contentWindow)setToolFrameReady($("#spreadsheetFrame"))});

init();